# 📖 دليل المستخدم الشامل - Fel7o Media Pro

يوضح هذا الدليل كيفية الاستفادة الكاملة من مميزات البوت المتقدمة، بما في ذلك البحث المضمن (Inline Mode)، تحميل البودكاست، وتشغيل البوت على سيرفر خارجي (VPS) بدون توقف.

---

## 1. 🔍 كيف تستخدم خاصية Inline Mode في أي شات على تليجرام؟

قبل الاستخدام، فعّل Inline Mode من `@BotFather` باستخدام الأمر `/setinline` واختر البوت ثم اكتب وصفاً مثل `Search music`.

تتيح لك هذه الميزة البحث عن أي أغنية أو تراك وإرسالها لأصدقائك في أي محادثة خاصة أو مجموعة **دون الحاجة لفتح شات البوت أصلاً**!

### خطوات الاستخدام:
1. افتح أي شات (محادثة شخصية أو جروب).
2. في صندوق كتابة الرسالة، اكتب معرف بوتك متبوعاً بمسافة ثم اسم الأغنية أو الفنان، هكذا:
   ```text
   @fel7o_media_pro_bot Blinding Lights
   ```
3. ستظهر لك فوراً قائمة منبثقة فوق صندوق الكتابة تضم نتائج البحث مرفقة بأغلفة الألبومات وأسماء الفنانين (نفس طريقة `deezload2bot`).
4. اضغط على أي نتيجة، وسيتم إرسالها فوراً في الشات لمن تحادثه!

---

## 2. 📁 تنزيل الألبومات وقوائم التشغيل

أرسل رابط ألبوم أو Playlist إلى البوت. سيعرض عدد الأغاني ثم يمنحك خيارات تنزيل الكل أو اختيار أغنية محددة أو الإلغاء. الحد الأقصى لكل دفعة هو 25 أغنية وتعمل الدفعة بالتتابع.

## 3. 🟢 متابعة أغاني Spotify المفضلة

بعد ربط Spotify، يفحص البوت المفضلة كل 6 ساعات. أول فحص يسجل الحالة فقط، وبعده تظهر رسالة اقتراح تحميل لكل أغنية جديدة. تتطلب هذه الميزة حساب Premium نشطاً لحساب مالك تطبيق Spotify وفق سياسة Spotify.

## 4. 🎙️ كيفية تحميل حلقات البودكاست (Podcasts)

أصبح البوت يدعم تحميل حلقات البودكاست من منصات مثل **Spotify Podcasts** و **Apple Podcasts** و **YouTube Podcasts**.

### خطوات التحميل:
1. انسخ رابط حلقة البودكاست من تطبيق سبوتيفاي (مثل رابط Episode أو Show).
2. الصق الرابط مباشرة في شات البوت.
3. سيتعرف البوت تلقائياً على أنه بودكاست، ويبدأ في تحميل الحلقة بصيغة `MP3` بجودة نقية مع وضع عنوان الحلقة وإرسالها لك.

---

## 5. 🖥️ تشغيل البوت على سيرفر خارجي (VPS) 24 ساعة بدون توقف

لكي يبقى البوت أونلاين طوال الوقت ولا يتوقف عند إغلاق جهازك، يمكنك حجزه على سيرفر VPS (مثل Ubuntu على DigitalOcean أو Hetzner أو Oracle Cloud المجاني) واتباع الخطوات الآتية:

### الخطوة الأولى: تجهيز السيرفر
اتصل بسيرفر الـ VPS عبر SSH، ثم قم بتثبيت بايثون وحزم الـ FFmpeg:
```bash
sudo apt update && sudo apt upgrade -y
sudo apt install python3-pip python3-venv ffmpeg git -y
```

### الخطوة الثانية: رفع المشروع
1. انسخ مشروعك إلى السيرفر (أو ارفعه عبر Git أو الملف المضغوط).
2. ادخل إلى مجلد المشروع وأنشئ البيئة الافتراضية:
   ```bash
   python3 -m venv venv
   source venv/bin/activate
   pip install -r requirements.txt
   ```

### الخطوة الثالثة: ضبط متغيرات البيئة
تأكد من وجود ملف `.env` في مجلد المشروع ويحتوي على التوكن ومفاتيحك:
```env
BOT_TOKEN=your_telegram_bot_token
BOT_USERNAME=your_bot_username
SPOTIFY_CLIENT_ID=your_spotify_client_id
SPOTIFY_CLIENT_SECRET=your_spotify_client_secret
ADMIN_CHAT_ID=رقم_الأي_دي_الخاص_بك
```

### الخطوة الرابعة: تشغيل البوت كخدمة دائمية (Systemd Service)
لكي يعمل البوت في الخلفية ويعيد تشغيل نفسه تلقائياً لو السيرفر رستر:
1. أنشئ ملف خدمة جديد:
   ```bash
   sudo nano /etc/systemd/system/fel7omediarpro.service
   ```
2. الصق الكود الآتي (مع تعديل مسار مجلد المشروع `/path/to/Fel7oMediaPro` ليطابق مسارك الحقيقي):
   ```ini
   [Unit]
   Description=Fel7o Media Pro Telegram Bot
   After=network.target

   [Service]
   User=root
   WorkingDirectory=/root/Fel7oMediaPro
   ExecStart=/root/Fel7oMediaPro/venv/bin/python main.py
   Restart=always
   RestartSec=10

   [Install]
   WantedBy=multi-user.target
   ```
3. احفظ الملف (`Ctrl+O` ثم `Enter` ثم `Ctrl+X`).
4. فعّل الخدمة وشغلها بالأوامر التالية:
   ```bash
   sudo systemctl daemon-reload
   sudo systemctl start fel7omediarpro
   sudo systemctl enable fel7omediarpro
   ```
5. لتفقد حالة البوت والتأكد أنه يعمل بنجاح:
   ```bash
   sudo systemctl status fel7omediarpro
   ```

مبروك! البوت الآن يعمل على السيرفر 24 ساعة بدون توقف وبأعلى أداء ممكن! 🚀🔥

## التشغيل على هذا الجهاز (Windows)

- تم إعداد مهمة تلقائية باسم `Fel7oMediaProBot`؛ تبدأ البوت بعد تسجيل الدخول وتعيد المحاولة عند الفشل.
- لتشغيله يدوياً استخدم `Start_Fel7o_Media.bat` من داخل مجلد المشروع، ثم راجع `bot-runtime.log` أو `bot-runtime-error.log` عند الحاجة.
- يظل البوت عاملاً ما دام الجهاز في وضع التشغيل؛ يلزم استضافة خارجية إذا أردته متاحاً والجهاز مغلق.

## الروابط وقائمة الانتظار

- أرسل رابط أغنية من Spotify أو YouTube مباشرة إلى البوت؛ يعالج Spotify البيانات ثم يجد المصدر الصوتي المناسب.
- روابط الفيديو القصير والبودكاست لها مسارها الخاص، والألبومات والقوائم تعرض تأكيداً قبل بدء الدفعة.
- كل طلب يدخل قائمة انتظار مع ترتيب وزر إلغاء. لا ينفذ البوت تنزيلين في الوقت نفسه لنفس المستخدم.
- الجودة الافتراضية هي `FLAC` بأفضل مصدر صوت متاح؛ يمكن التبديل إلى `320kbps MP3` من الإعدادات إذا أردت ملفات أصغر.

## Fel7o Inbox داخل جروب Telegram

1. أنشئ جروباً خاصاً باسم `Fel7o Inbox` وأضف البوت إليه.
2. من `@BotFather` استخدم `/setprivacy`، اختر البوت ثم اختر `Disable` حتى يستقبل روابط Spotify وYouTube المرسلة إلى الجروب. يمكن أيضاً جعله مشرفاً في الجروب.
3. شارك أي رابط أغنية من Spotify أو YouTube إلى الجروب، أو أرسله مباشرة إلى البوت؛ يعرض البوت بطاقة فيها **تحميل / حفظ / تجاهل** بدلاً من بدء تنزيل تلقائي.
4. أرسل `/inbox` داخل الجروب أو في الشات الخاص لرؤية آخر الأغاني المحفوظة للمكان الحالي.
5. من الجروب، زر **أرسل لي خاص** يرسل التحميل إلى صاحب الطلب في الخاص؛ يجب أن يكون قد أرسل `/start` للبوت مرة واحدة.
6. زر **الأخيرة** أو الأمر `/recent` يعرض آخر التحميلات مع زر إعادة تحميل لكل أغنية.

## متابعة Fel7o Inbox من Spotify

1. أنشئ Playlist في Spotify باسم `Fel7o Inbox` وأضف إليها الأغاني التي تريد متابعتها.
2. من البوت: اربط حساب Spotify، ثم اختر زر `ربط Playlist للمتابعة`.
3. أرسل: `/watch_playlist ` متبوعاً برابط الـPlaylist المنسوخ من Spotify.
4. أول فحص ينشئ خط أساس بلا رسائل؛ بعدها يرسل البوت إشعاراً بالأغاني المضافة حديثاً كل 6 ساعات.
