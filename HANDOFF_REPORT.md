# Fel7o Media Pro — تقرير التسليم

تاريخ التسليم: 16 أغسطس 2026

## حالة المشروع

البوت مكتوب بـ Python ويعمل عبر `pyTelegramBotAPI`. نقطة البداية هي `main.py`، والتنزيل يتم عبر `yt-dlp`، بينما ربط Spotify موجود في `spotify_auth.py` باستخدام OAuth وSpotipy.

آخر تشغيل محلي وصل بنجاح إلى:

```text
[Fel7o Media Pro] Ready! Bot: @fel7o_media_pro_bot
```

نسخة رجوع قبل التحديثات النهائية موجودة في:

```text
D:\Games\Fel7oMediaPro-final-backup-20260815-235130
```

## التشغيل

1. أنشئ `.env` من `.env.example` وأضف القيم المطلوبة.
2. ثبّت الاعتمادات: `pip install -r requirements.txt`.
3. تأكد من توفر `ffmpeg` على النظام.
4. شغّل: `python main.py`.

متغيرات البيئة:

- `BOT_TOKEN` مطلوب.
- `BOT_USERNAME` اختياري، ويكتشف البوت الاسم تلقائياً عند التشغيل.
- `SPOTIFY_CLIENT_ID` و`SPOTIFY_CLIENT_SECRET` اختياريان لربط Spotify.
- `SPOTIFY_REDIRECT_URI` افتراضيه `http://127.0.0.1:9876/callback`.
- `SPOTIFY_POLL_INTERVAL_SECONDS` افتراضيه `21600` (6 ساعات).
- `ADMIN_CHAT_ID` و`DEFAULT_BITRATE` إعدادات اختيارية.

## المزايا المنفذة

### البحث المضمن

- أوضاع البحث: أغنية، ألبوم، Playlist، فنان، Label، وبحث شامل.
- نتائج على صفحات من 8 عناصر مع صورة غلاف، اسم الأغنية، الفنان، الألبوم والمدة.
- إن لم يعط المصدر غلافاً، يستخدم البوت غلاف YouTube عالي الجودة كاحتياط.
- الضغط على نتيجة ينشر بطاقة صغيرة؛ زر التحميل يفتح شات البوت عبر Deep Link قصير الصلاحية ثم يبدأ التنزيل بالجودة المحفوظة للمستخدم.
- Telegram هو من يرسم القائمة المنزلقة؛ الشكل المرجعي (غلاف يسار ثم العنوان والفنان والألبوم) يعتمد على `title` و`description` و`thumbnail_url` المرسلة من البوت.

### الروابط والتنزيل

- الروابط الفردية للصوت والفيديو والبودكاست تعمل عبر مسارات مستقلة.
- روابط الألبومات وPlaylists لا تبدأ التحميل تلقائياً: تظهر أزرار تنزيل الكل، اختيار أغنية، أو إلغاء.
- أقصى دفعة 25 أغنية، وتعمل الأغاني بالتتابع مع رسالة تقدم وملخص نجاح/فشل.
- ذاكرة الدفعات ونتائج البحث لها مدة صلاحية لمنع الروابط القديمة أو استخدام نتائج مستخدم آخر.

### Spotify

- OAuth لكل مستخدم، وملفات توكن مستقلة في `data/sp_token_<chat_id>.json`.
- أول فحص ناجح للمفضلة يبني خط أساس بدون إرسال كل الأغاني القديمة.
- بعد ذلك، أي أغنية جديدة في المفضلة ترسل بطاقة اقتراح تحميل مع زر تحميل/تجاهل.
- الفحص كل 6 ساعات، وتظهر الحالة وآخر فحص داخل الإعدادات.
- عند رفض Spotify أو انتهاء التفويض، يرسل البوت تنبيهاً واحداً واضحاً ثم يعيد المحاولة في الدورة التالية.
- بديل المفضلة: يدعم البوت الآن متابعة Playlist مرتبطة لكل مستخدم عبر الأمر `/watch_playlist <spotify-playlist-url>`؛ أول فحص صامت ثم إشعار بالجديد كل 6 ساعات.

## ملاحظة حاسمة: Inline Mode

الكود جاهز، لكن يجب تفعيل Inline Mode من BotFather حتى تظهر القائمة المنسدلة:

1. افتح `@BotFather`.
2. أرسل `/setinline`.
3. اختر `@fel7o_media_pro_bot`.
4. أرسل الوصف: `Search music`.

محاولة الأتمتة عبر Telegram Web لم تستكمل الحفظ بسبب تذبذب واجهة الويب؛ هذه هي الخطوة اليدوية الوحيدة المتبقية.

## قيود معروفة

- Spotify رفض حالياً الوصول إلى المفضلة برمز `403` لأن حساب مالك تطبيق Spotify يحتاج Premium نشطاً. لا يوجد حل برمجي آمن لتجاوز هذا الشرط.
- لا تستخدم Web Scraping أو Cookies لصفحة Spotify المفضلة؛ المتابعة مبنية على Spotify API الرسمي فقط.
- تنزيل الوسائط يعتمد على إتاحة المصدر و`ffmpeg` وحدود Telegram لحجم الملفات.
- تم تسجيل مهمة Windows باسم `Fel7oMediaProBot` لتشغيل البوت تلقائياً بعد تسجيل دخول المستخدم، مع إعادة محاولة ثلاث مرات عند الفشل. ملف `Start_Fel7o_Media.bat` يعمل من مجلد المشروع الصحيح ويكتب السجلين `bot-runtime.log` و`bot-runtime-error.log` عند تشغيله يدوياً.

## التحقق المنفذ

- `python -m compileall -q .` نجح.
- اختبار Cache لنتائج البحث وصلاحيتها نجح.
- اختبار حد دفعات Playlist/Album وملكية المستخدم نجح.
- اختبار Spotify: خط الأساس لا يرسل إشعاراً؛ إضافة أغنية لاحقة ترسل إشعاراً.
- اختبار منع تكرار رسائل أخطاء Spotify نجح.
- تم التحقق من بدء البوت محلياً وظهور رسالة الجاهزية.
- تم اختبار قائمة الانتظار: تمنع تنزيلين متزامنين لنفس المستخدم، تعرض ترتيبه، وتلغي الطلبات التي لم تبدأ بعد.
- تم تشغيل مهمة `Fel7oMediaProBot` والتحقق من أن عملية البوت تعمل من خلالها.

## نقاط البداية للمطور القادم

- `main.py`: أوامر Telegram، البحث المضمن، تنزيل الروابط والدفعات.
- `search_engine.py`: بحث المصدر واستخراج Metadata والغلاف.
- `spotify_auth.py`: OAuth، متابعة المفضلة، وحالة Spotify.
- `downloader.py`: إعدادات `yt-dlp`، تحويل الصوت وبياناته.
- `USER_GUIDE.md` و`DEPLOYMENT.md`: التشغيل والنشر للمستخدم.

## أولويات مقترحة لاحقاً

1. تفعيل Inline Mode واختبار القائمة في تطبيق Telegram الحقيقي.
2. إضافة Paging لاختيار أغنية من Playlist عندما تكون 25 أغنية لتقليل طول لوحة الأزرار.
3. نقل التخزين من JSON إلى SQLite إذا زاد عدد المستخدمين أو الدفعات.
4. نقل التشغيل إلى استضافة دائمة خارج الجهاز إذا كان المطلوب أن يعمل حتى والجهاز مغلق.
